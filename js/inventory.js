
function renderInventory(){ const loc=byId('invLoc').value, cat=byId('invCat').value, q=byId('invSearch').value.toLowerCase(); const rows=state.products.filter(p=>(!cat||p.category===cat)&&(!q||p.name.toLowerCase().includes(q))).map(p=>{ const c=stockCell(p,loc); const cur=currentQty(p,loc); const status=cur<0?'<span class="pill danger">음수</span>':cur<=Number(c.base||0)?'<span class="pill warn">기준 이하</span>':'<span class="pill ok">정상</span>'; return `<tr><td>${p.category}</td><td>${p.name}</td><td>${qty(c.stock)}</td><td>${qty(cur)}</td><td>${qty(c.in)}</td><td>${qty(c.use)}</td><td>${qty(c.prepaid)}</td><td>${qty(c.base)}</td><td>${status}</td></tr>`; }); byId('inventoryTable').innerHTML=rows.join('')||'<tr><td colspan="9" class="empty">조회 결과가 없습니다.</td></tr>'; }
function handleInventoryUpload(e){ const file=e.target.files[0]; if(!file) return; readWorkbook(file, rows=>{ let ok=0; rows.forEach(r=>{ const name=r['품목명']; const loc=r['위치']; if(!name||!loc) return; let p=state.products.find(x=>x.name===name); if(!p){p={id:'P'+String(state.products.length+1).padStart(3,'0'),name,category:r['제품카테고리']||'미분류',purchasePrice:0,salePrices:{'경북대':0,'영대':0,'동산':0,'기타':0},stock:{}}; state.products.push(p);} p.stock[loc]=p.stock[loc]||{}; p.stock[loc].stock=parseNumber(r['재고수량']); p.stock[loc].base=parseNumber(r['기준재고']||r['재고수량']); ok++; }); addHistory('재고업로드',file.name,ok); renderInventory(); byId('invUploadResult').innerHTML=`<span class="pill ok">${ok}건 반영</span>`; }); }
function exportInventoryCSV(){ const loc=byId('invLoc').value; const rows=[['위치','ID','제품카테고리','품목명','재고','현재고','기준재고']]; state.products.forEach(p=>rows.push([loc,p.id,p.category,p.name,stockCell(p,loc).stock,currentQty(p,loc),stockCell(p,loc).base])); download('inventory.csv',toCSV(rows),'text/csv;charset=utf-8'); }
document.addEventListener('DOMContentLoaded',()=>{ locationOptions(byId('invLoc')); categoryOptions(byId('invCat')); renderInventory(); });
