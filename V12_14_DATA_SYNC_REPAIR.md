# v12.14 병원 Master 및 Cloud 데이터 보호

## 원인

새 브라우저에서 생성된 빈 초기 데이터의 시간이 기존 Cloud 데이터보다 최신으로 기록되면서, 빈 데이터가 Cloud 원본보다 우선 저장될 수 있었습니다.

## 수정

- Cloud에 기존 데이터가 있으면 Cloud 원본을 우선 불러옵니다.
- 실제로 로컬에서 편집했지만 아직 전송하지 못한 경우만 로컬 데이터를 보호합니다.
- Cloud 저장 성공 여부를 별도로 기록합니다.
- 이전 LocalStorage에 병원 또는 병원별 단가가 남아 있으면 누락된 Master 정보를 자동 복구합니다.
- 모든 페이지의 Cloud 관련 JavaScript 캐시 버전을 갱신합니다.

## 기존 병원 정보가 이미 Cloud에서 빈 값으로 덮인 경우

같은 브라우저의 이전 LocalStorage 또는 JSON 백업에 데이터가 남아 있으면 자동 복구되거나 JSON 복원으로 되살릴 수 있습니다. 두 곳 모두 없다면 Supabase Database Backup에서 덮어쓰기 전 시점의 데이터를 복원해야 합니다.
