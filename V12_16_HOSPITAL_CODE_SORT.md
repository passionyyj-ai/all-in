# v12.16 병원코드 기준 정렬

- 병원 Master 목록을 병원코드 자연 정렬 순서로 표시
- 거래관리, 재고관리, 병원별 단가, 단가관리 및 재고 감사의 병원 선택 목록에도 동일한 순서 적용
- 숫자가 포함된 코드도 `001, 002, 010` 순서로 정렬
- 병원코드가 없는 데이터는 코드가 있는 병원 뒤에 표시
- 신규 저장, 수정 및 Excel 업로드 직후에도 즉시 재정렬
